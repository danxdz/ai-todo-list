# Optional Render Blueprint — create a Web Service from this repo.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: fruit-merge-demo
    runtime: python
    plan: free
    buildCommand: "true"
    startCommand: python run_fruit_merge_demo.py
